// Search-specific tuning and defaults.
export const RecentlyUsedSearchTuning = {
    SECTION_SIGNATURE_MAX_SAMPLES: 60,
    MAX_VALUE_COLLECTION_DEPTH: 3,
    MAX_ARRAY_ENTRIES_PER_LEVEL: 50,
};

// Canonical keys often containing meaningful search text.
export const RecentlyUsedSearchDefaults = {
    PREFERRED_KEYS: ['preview', 'value', 'char', 'name', 'description', 'title', 'subtitle', 'text', 'content', 'label'],
};
